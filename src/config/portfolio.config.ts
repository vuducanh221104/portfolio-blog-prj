export const portfolioConfig = {
  // Basic Information
  name: "Vũ Đức Anh",
  title: "Fullstack Developer",
  description: "I am a Fullstack Developer",
  location: "Ho Chi Minh City, Vietnam",

  // Contact Information
  email: "vng15960@gmail.com",

  // Social Media Links
  socialLinks: {
    facebook: "https://www.facebook.com/I.Am.DucAnh123",
    twitter: "https://www.facebook.com/I.Am.DucAnh123",
    linkedin: "https://www.linkedin.com/in/duc-anh-vu-61487b308?utm_source=share_via&utm_content=profile&utm_medium=member_ios",
    external: "https://zalo.me/0377775528",
    github: "https://github.com/vuducanh221104",
  },

  // SEO Information
  seo: {
    ogImage: "https://mdtaquiimam.vercel.app/og-image.png",
    url: "https://mdtaquiimam.vercel.app",
    twitterHandle: "@vuducanh",
    keywords: [
      "portfolio",
      "web developer",
      "web",
      "web dev",
      "developer",
      "PROGRAMMER ",
      "programmer ",
      "website",
    ],
    authors: [
      {
        name: "Vũ Đức Anh",
        url: "https://github.com/vuducanh221104",
      },
    ],
  },

  // About Information
  about: {
    bio: "I’m a 4th-year student specializing in full-stack development with a strong focus on code quality, performance, and user experience. I’m interested in building reliable, scalable web applications and improving my skills in backend development, testing, and deployment. I’m seeking an internship/junior opportunity to contribute and grow with a professional engineering team.",
    hobbies: [
      "Coding",
      "Playing Games",
      "Soccer",
      "Reading",
      "Gym",
    ],
    personalInfo: {
      language: "Vietnamese, English ",
      nationality: "Vietnam",
      gender: "Male",
    },
  },

  // Skills and Roles
  skills: {
    roles: [
      "Freelancer",
      "Blogger",
      "Devops",
      "Creator",
      "Student",
      "Contributer",
    ],
    // Technical Skills
    programmingLanguages: [

      {
        name: "Swift",
        icon: "https://cdn-icons-png.flaticon.com/512/732/732250.png",
      },
      {
        name: "HTML5",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/html5/html5-original.svg",
      },
      {
        name: "CSS3",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/css3/css3-original.svg",
      },
      {
        name: "JavaScript",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/javascript/javascript-original.svg",
      },
      {
        name: "TypeScript",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/typescript/typescript-original.svg",
      },
      {
        name: "C++",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/cplusplus/cplusplus-original.svg",
      },
      {
        name: "Python",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/python/python-original.svg",
      },
    ],
    frameworks: [
      {
        name: "React",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/react/react-original.svg",
      },
      {
        name: "Next.js",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/nextjs/nextjs-original.svg",
      },
      {
        name: "Vue.js",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/vuejs/vuejs-original.svg",
      },
      {
        name: "ShadcnUI",
        icon: "https://media.daily.dev/image/upload/s--nmy-SeMz--/f_auto/v1733385075/squads/f6e60edb-ee79-4dee-914c-841e14638aa9",
      },
      {
        name: "Tailwind CSS",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/tailwindcss/tailwindcss-original.svg",
      },
      {
        name: "Bootstrap",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/bootstrap/bootstrap-original.svg",
      },
      {
        name: "Material UI",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/materialui/materialui-original.svg",
      },
    ],
    tools: [
      {
        name: "Git",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/git/git-original.svg",
      },
      {
        name: "GitHub",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/github/github-original.svg",
      },
      {
        name: "VS Code",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/vscode/vscode-original.svg",
      },
      {
        name: "Docker",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/docker/docker-original.svg",
      },
      {
        name: "Firebase",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/firebase/firebase-plain.svg",
      },
      {
        name: "MongoDB",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/mongodb/mongodb-original.svg",
      },
      {
        name: "PostgreSQL",
        icon: "https://cdn.jsdelivr.net/gh/devicons/devicon/icons/postgresql/postgresql-original.svg",
      },
    ],
  },

  // Education Information
  education: [
    {
      degree: "IT - Information Technology",
      institution: "Hutech University" ,
      location: "Ho Chi Minh City",
      period: "2022 - 2026",
      description:
        "I am currently Studying IT - Information Technology form Hutech University. The program has provided me with a well-rounded education, covering both theoretical foundations and practical applications of computer science.",
    },
  ],

  // Projects Information
  projects: [
    {
      title: "Luxe Wear AI - AI AGENT",
      description:
        "A sophisticated SaaS platform empowering businesses to deploy autonomous AI agents. Features real-time data integration, multi-system orchestration, and detailed analytical reporting.",
      tags: ["Next.js 14", "Gemini AI", "Vector DB", "Redis","Supabase","ExpressJS"],
      link: "https://github.com/vuducanh221104/client-luxe-wear-ai",
      image: "/project-image/luxe-wear.png",
    },
    {
      title: "Shop GYM- ZenFit",
      description:
      "A modern, responsive e-commerce platform designed to streamline online shopping for gym equipment and apparel. Features include product catalog, shopping cart functionality, secure checkout, and a user-friendly interface.",
      tags: ["Nextjs", "Typescript", "Material UI", "SCSS","ExpressJS","MongoDB" ],
      link: "https://github.com/vuducanh221104/client-ecommerce-shop",
      image: "/project-image/zenfit.png",
    },
  ],

  // Blog Posts Information
  blogPosts: [
    {
      title: "Javascript là gì",
      description: "Tìm hiểu về JavaScript - ngôn ngữ lập trình phổ biến nhất thế giới, được sử dụng để tạo ra các trang web tương tác và ứng dụng web hiện đại.",
      author: "Vũ Đức Anh",
      date: "January 20, 2025",
      readTime: "10 min read",
      tags: ["JavaScript", "Programming", "Web Development"],
      slug: "javascript-la-gi",
      image: "/blog/javascript-la-gi.png",
      content: `
        <h2>Giới thiệu về JavaScript</h2>
        <p>JavaScript là một ngôn ngữ lập trình động, được phát triển bởi Brendan Eich vào năm 1995. Ban đầu, JavaScript được tạo ra chỉ trong 10 ngày với mục đích làm cho các trang web trở nên tương tác hơn. Ngày nay, JavaScript đã trở thành một trong những ngôn ngữ lập trình phổ biến nhất thế giới, được sử dụng bởi hàng triệu developer trên toàn cầu.</p>

        <h2>JavaScript là gì?</h2>
        <p>JavaScript (thường được viết tắt là JS) là một ngôn ngữ lập trình high-level, được thông dịch (interpreted) và có kiểu động (dynamically typed). JavaScript được thiết kế để tạo ra các trang web tương tác và động. Nó là một trong ba công nghệ cốt lõi của World Wide Web, cùng với HTML và CSS.</p>

        <h2>Đặc điểm của JavaScript</h2>
        <h3>1. Ngôn ngữ thông dịch (Interpreted Language)</h3>
        <p>JavaScript không cần được biên dịch trước khi chạy. Trình duyệt web sẽ đọc và thực thi code JavaScript trực tiếp. Điều này giúp việc phát triển trở nên nhanh chóng và linh hoạt hơn.</p>

        <h3>2. Kiểu động (Dynamic Typing)</h3>
        <p>Trong JavaScript, bạn không cần khai báo kiểu dữ liệu cho biến. Kiểu dữ liệu sẽ được xác định tự động khi chạy chương trình. Ví dụ:</p>
        <pre><code>let x = 10;        // x là number
let y = "Hello";   // y là string
let z = true;      // z là boolean</code></pre>

        <h3>3. Hướng đối tượng (Object-Oriented)</h3>
        <p>JavaScript hỗ trợ lập trình hướng đối tượng thông qua prototypes. Bạn có thể tạo các object và sử dụng inheritance, encapsulation, và polymorphism.</p>

        <h3>4. Hàm là First-Class Citizens</h3>
        <p>Trong JavaScript, hàm có thể được gán cho biến, truyền như tham số, hoặc trả về từ hàm khác. Điều này cho phép lập trình hàm (functional programming) rất mạnh mẽ.</p>

        <h2>JavaScript chạy ở đâu?</h2>
        <h3>1. Trình duyệt (Browser)</h3>
        <p>JavaScript ban đầu được thiết kế để chạy trong trình duyệt web. Nó có thể tương tác với DOM (Document Object Model) để thay đổi nội dung, style, và cấu trúc của trang web.</p>

        <h3>2. Server (Node.js)</h3>
        <p>Với sự ra đời của Node.js vào năm 2009, JavaScript giờ đây có thể chạy trên server. Điều này cho phép developer sử dụng cùng một ngôn ngữ cho cả frontend và backend.</p>

        <h3>3. Mobile Apps</h3>
        <p>Với các framework như React Native, Ionic, và NativeScript, bạn có thể sử dụng JavaScript để phát triển ứng dụng mobile cho cả iOS và Android.</p>

        <h3>4. Desktop Apps</h3>
        <p>Electron cho phép bạn xây dựng ứng dụng desktop bằng JavaScript, HTML, và CSS. Các ứng dụng nổi tiếng như VS Code, Slack, và Discord đều được xây dựng bằng Electron.</p>

        <h2>Cú pháp cơ bản của JavaScript</h2>
        <p>JavaScript có cú pháp tương tự như Java và C, nhưng linh hoạt hơn nhiều. Dưới đây là một số ví dụ cơ bản:</p>

        <h3>Khai báo biến</h3>
        <pre><code>// Sử dụng let (khuyến nghị)
let name = "Vũ Đức Anh";
let age = 20;

// Sử dụng const cho hằng số
const PI = 3.14159;

// Sử dụng var (không khuyến nghị trong code hiện đại)
var oldWay = "deprecated";</code></pre>

        <h3>Hàm (Functions)</h3>
        <pre><code>// Function declaration
function greet(name) {
  return "Hello, " + name + "!";
}

// Arrow function (ES6+)
const greetArrow = (name) => {
  return \`Hello, \${name}!\`;
};

// Arrow function ngắn gọn
const add = (a, b) => a + b;</code></pre>

        <h3>Objects và Arrays</h3>
        <pre><code>// Object
const person = {
  name: "Vũ Đức Anh",
  age: 20,
  city: "Ho Chi Minh City"
};

// Array
const fruits = ["Apple", "Banana", "Orange"];

// Array methods
fruits.forEach(fruit => console.log(fruit));
const upperFruits = fruits.map(fruit => fruit.toUpperCase());</code></pre>

        <h2>ES6+ và các tính năng hiện đại</h2>
        <p>ECMAScript 6 (ES6) được phát hành vào năm 2015, mang đến nhiều tính năng mới quan trọng:</p>
        <ul>
          <li><strong>Arrow Functions:</strong> Cú pháp ngắn gọn hơn cho hàm</li>
          <li><strong>Template Literals:</strong> Cho phép nhúng biến vào chuỗi</li>
          <li><strong>Destructuring:</strong> Trích xuất giá trị từ objects và arrays</li>
          <li><strong>Spread Operator:</strong> Mở rộng arrays và objects</li>
          <li><strong>Promises và Async/Await:</strong> Xử lý bất đồng bộ dễ dàng hơn</li>
          <li><strong>Classes:</strong> Cú pháp OOP rõ ràng hơn</li>
          <li><strong>Modules:</strong> Import/export code giữa các file</li>
        </ul>

        <h2>Tại sao nên học JavaScript?</h2>
        <ol>
          <li><strong>Dễ học:</strong> Cú pháp đơn giản, phù hợp cho người mới bắt đầu</li>
          <li><strong>Phổ biến:</strong> Được sử dụng rộng rãi trong ngành công nghiệp</li>
          <li><strong>Đa nền tảng:</strong> Có thể phát triển web, mobile, desktop, và server</li>
          <li><strong>Cộng đồng lớn:</strong> Nhiều tài nguyên học tập và hỗ trợ</li>
          <li><strong>Cơ hội việc làm:</strong> Nhu cầu tuyển dụng JavaScript developer rất cao</li>
          <li><strong>Ecosystem phong phú:</strong> Nhiều thư viện và framework mạnh mẽ</li>
        </ol>

        <h2>Kết luận</h2>
        <p>JavaScript là một ngôn ngữ lập trình mạnh mẽ và linh hoạt, đóng vai trò quan trọng trong phát triển web hiện đại. Từ việc tạo ra các trang web tương tác đơn giản đến xây dựng các ứng dụng phức tạp, JavaScript đều có thể đáp ứng. Nếu bạn đang bắt đầu hành trình lập trình, JavaScript là một lựa chọn tuyệt vời!</p>

        <p>Hãy bắt đầu học JavaScript ngay hôm nay và khám phá thế giới lập trình web đầy thú vị. Chúc bạn thành công!</p>
      `,
    },
    {
      title: "Ứng dụng của JavaScript",
      description: "Khám phá các ứng dụng thực tế của JavaScript trong phát triển web, từ frontend đến backend, và các framework hiện đại.",
      author: "Vũ Đức Anh",
      date: "January 18, 2025",
      readTime: "12 min read",
      tags: ["JavaScript", "Applications", "Web Development"],
      slug: "ung-dung-cua-javascript",
      image: "/blog/ung-dung-cua-Javascript.jpg",
      content: `
        <h2>Giới thiệu</h2>
        <p>JavaScript không chỉ là một ngôn ngữ lập trình đơn giản cho web. Ngày nay, nó đã trở thành một công cụ mạnh mẽ được sử dụng trong nhiều lĩnh vực khác nhau, từ phát triển web đến ứng dụng mobile, từ game development đến machine learning. Trong bài viết này, chúng ta sẽ khám phá các ứng dụng thực tế và phổ biến nhất của JavaScript.</p>

        <h2>1. Phát triển Web Frontend</h2>
        <h3>Trang web tương tác</h3>
        <p>Ứng dụng cơ bản nhất của JavaScript là tạo ra các trang web tương tác. JavaScript cho phép bạn:</p>
        <ul>
          <li>Thay đổi nội dung trang web động mà không cần reload</li>
          <li>Xử lý sự kiện người dùng (click, hover, scroll, v.v.)</li>
          <li>Validate form trước khi submit</li>
          <li>Tạo animations và transitions mượt mà</li>
          <li>Tương tác với APIs để lấy và hiển thị dữ liệu</li>
        </ul>

        <h3>Single Page Applications (SPA)</h3>
        <p>Với các framework như React, Vue.js, và Angular, JavaScript cho phép tạo ra các Single Page Applications - ứng dụng web hoạt động như một ứng dụng desktop, không cần reload trang khi điều hướng.</p>

        <h2>2. Phát triển Backend với Node.js</h2>
        <p>Node.js đã cách mạng hóa cách chúng ta nghĩ về JavaScript. Giờ đây, bạn có thể sử dụng JavaScript để:</p>

        <h3>Xây dựng RESTful APIs</h3>
        <p>Node.js cùng với Express.js cho phép tạo ra các API server mạnh mẽ và hiệu quả. Ví dụ:</p>
        <pre><code>const express = require('express');
const app = express();

app.get('/api/users', (req, res) => {
  res.json({ users: [...] });
});

app.listen(3000, () => {
  console.log('Server running on port 3000');
});</code></pre>

        <h3>Real-time Applications</h3>
        <p>Với Socket.io, bạn có thể tạo các ứng dụng real-time như chat apps, collaboration tools, hoặc live dashboards.</p>

        <h3>Microservices</h3>
        <p>Node.js rất phù hợp cho kiến trúc microservices nhờ tính nhẹ và khả năng xử lý I/O tốt.</p>

        <h2>3. Ứng dụng Mobile</h2>
        <h3>React Native</h3>
        <p>React Native cho phép bạn viết ứng dụng mobile cho cả iOS và Android bằng JavaScript. Các ứng dụng nổi tiếng như Facebook, Instagram, và Uber Eats đều sử dụng React Native.</p>

        <h3>Ionic</h3>
        <p>Ionic sử dụng web technologies (HTML, CSS, JavaScript) để tạo ứng dụng mobile với performance gần như native.</p>

        <h3>NativeScript</h3>
        <p>NativeScript cho phép truy cập trực tiếp vào native APIs của iOS và Android, cho phép tạo ra các ứng dụng với performance cao.</p>

        <h2>4. Ứng dụng Desktop</h2>
        <h3>Electron</h3>
        <p>Electron cho phép xây dựng ứng dụng desktop đa nền tảng bằng JavaScript, HTML, và CSS. Các ứng dụng nổi tiếng được xây dựng bằng Electron bao gồm:</p>
        <ul>
          <li><strong>Visual Studio Code:</strong> Code editor phổ biến nhất thế giới</li>
          <li><strong>Slack:</strong> Ứng dụng team collaboration</li>
          <li><strong>Discord:</strong> Voice và text chat cho gamers</li>
          <li><strong>Figma:</strong> Design tool (desktop app)</li>
          <li><strong>Spotify:</strong> Music streaming app</li>
        </ul>

        <h2>5. Game Development</h2>
        <p>JavaScript có thể được sử dụng để tạo ra các game web và mobile:</p>
        <ul>
          <li><strong>Phaser.js:</strong> Framework mạnh mẽ cho game 2D</li>
          <li><strong>Three.js:</strong> Thư viện 3D graphics cho web</li>
          <li><strong>Babylon.js:</strong> Game engine 3D đầy đủ tính năng</li>
          <li><strong>PixiJS:</strong> 2D WebGL renderer với performance cao</li>
        </ul>

        <h2>6. Machine Learning và AI</h2>
        <p>Với TensorFlow.js, bạn có thể chạy machine learning models trực tiếp trong browser hoặc Node.js:</p>
        <ul>
          <li>Image recognition</li>
          <li>Natural language processing</li>
          <li>Recommendation systems</li>
          <li>Predictive analytics</li>
        </ul>

        <h2>7. IoT (Internet of Things)</h2>
        <p>Với Node.js, bạn có thể lập trình cho các thiết bị IoT như Raspberry Pi, Arduino, và các smart devices khác.</p>

        <h2>8. Automation và Scripting</h2>
        <p>JavaScript có thể được sử dụng để tự động hóa các tác vụ:</p>
        <ul>
          <li><strong>Web Scraping:</strong> Với Puppeteer hoặc Playwright</li>
          <li><strong>Build Tools:</strong> Webpack, Gulp, Grunt</li>
          <li><strong>Testing:</strong> Jest, Mocha, Cypress</li>
          <li><strong>Task Automation:</strong> Tự động hóa các công việc lặp đi lặp lại</li>
        </ul>

        <h2>9. Data Visualization</h2>
        <p>JavaScript có nhiều thư viện mạnh mẽ cho data visualization:</p>
        <ul>
          <li><strong>D3.js:</strong> Thư viện mạnh mẽ nhất cho data visualization</li>
          <li><strong>Chart.js:</strong> Đơn giản và dễ sử dụng</li>
          <li><strong>Highcharts:</strong> Professional charts cho business</li>
          <li><strong>Plotly.js:</strong> Scientific và statistical charts</li>
        </ul>

        <h2>10. Blockchain và Cryptocurrency</h2>
        <p>JavaScript được sử dụng trong phát triển blockchain applications:</p>
        <ul>
          <li>Smart contracts với Solidity (có thể compile sang JavaScript)</li>
          <li>Web3.js và Ethers.js để tương tác với Ethereum blockchain</li>
          <li>Building DApps (Decentralized Applications)</li>
        </ul>

        <h2>11. Serverless Functions</h2>
        <p>Với các nền tảng như AWS Lambda, Google Cloud Functions, và Vercel, bạn có thể chạy JavaScript functions mà không cần quản lý server.</p>

        <h2>12. Command Line Tools</h2>
        <p>Node.js cho phép tạo các CLI tools mạnh mẽ. Các công cụ phổ biến được xây dựng bằng Node.js:</p>
        <ul>
          <li><strong>npm/yarn:</strong> Package managers</li>
          <li><strong>Create React App:</strong> Tool để tạo React projects</li>
          <li><strong>Next.js CLI:</strong> Framework cho React</li>
          <li><strong>Angular CLI:</strong> Command line interface cho Angular</li>
        </ul>

        <h2>Kết luận</h2>
        <p>JavaScript đã phát triển từ một ngôn ngữ đơn giản cho web thành một công cụ đa nền tảng mạnh mẽ. Từ frontend đến backend, từ web đến mobile và desktop, JavaScript đều có thể đáp ứng. Với ecosystem phong phú và cộng đồng lớn mạnh, JavaScript tiếp tục là một trong những ngôn ngữ lập trình quan trọng nhất trong ngành công nghệ.</p>

        <p>Cho dù bạn muốn trở thành frontend developer, backend developer, fullstack developer, hay mobile developer, việc thành thạo JavaScript sẽ mở ra nhiều cơ hội nghề nghiệp tuyệt vời. Hãy bắt đầu học và thực hành JavaScript ngay hôm nay!</p>

        <p><strong>Lời khuyên:</strong> Bắt đầu với JavaScript cơ bản, sau đó chọn một lĩnh vực bạn quan tâm (web, mobile, game, v.v.) và học sâu hơn về các framework và thư viện liên quan. Thực hành thường xuyên và xây dựng các dự án thực tế sẽ giúp bạn thành thạo JavaScript nhanh chóng.</p>
      `,
    },
    {
      title: "Next.js là gì? Hướng dẫn cho người mới bắt đầu",
      description: "Tìm hiểu về Next.js - framework React mạnh mẽ cho phép xây dựng ứng dụng web với Server-Side Rendering, Static Site Generation và nhiều tính năng hiện đại khác.",
      author: "Vũ Đức Anh",
      date: "January 25, 2025",
      readTime: "15 min read",
      tags: ["Next.js", "React", "Web Development", "Framework"],
      slug: "nextjs-la-gi",
      image: "/blog/nextjs_la_gi.jpg",
      content: `
        <h2>Giới thiệu về Next.js</h2>
        <p>Next.js là một framework React mã nguồn mở được phát triển bởi Vercel. Nó được xây dựng dựa trên React và cung cấp các tính năng mạnh mẽ như Server-Side Rendering (SSR), Static Site Generation (SSG), và API Routes. Next.js giúp bạn xây dựng các ứng dụng web production-ready với hiệu suất cao và trải nghiệm người dùng tuyệt vời.</p>

        <h2>Next.js là gì?</h2>
        <p>Next.js là một framework full-stack cho React, cho phép bạn xây dựng cả frontend và backend trong cùng một dự án. Nó giải quyết nhiều vấn đề phổ biến khi phát triển ứng dụng React như routing, code splitting, và optimization.</p>

        <h2>Tại sao nên sử dụng Next.js?</h2>
        <h3>1. Server-Side Rendering (SSR)</h3>
        <p>Next.js cho phép render React components trên server, giúp cải thiện SEO và thời gian load ban đầu. Điều này đặc biệt quan trọng cho các trang web cần được index tốt bởi search engines.</p>

        <h3>2. Static Site Generation (SSG)</h3>
        <p>Bạn có thể pre-render pages tại build time, tạo ra các file HTML tĩnh. Điều này giúp trang web load cực kỳ nhanh và có thể được host trên CDN.</p>

        <h3>3. File-based Routing</h3>
        <p>Next.js sử dụng file system để định nghĩa routes. Mỗi file trong thư mục <code>pages</code> hoặc <code>app</code> tự động trở thành một route.</p>
        <pre><code>pages/
  index.js          → /
  about.js          → /about
  blog/
    [slug].js       → /blog/:slug</code></pre>

        <h3>4. API Routes</h3>
        <p>Bạn có thể tạo API endpoints ngay trong Next.js mà không cần một server riêng biệt. Chỉ cần tạo file trong thư mục <code>pages/api</code> hoặc <code>app/api</code>.</p>
        <pre><code>// pages/api/users.js
export default function handler(req, res) {
  res.status(200).json({ users: [...] });
}</code></pre>

        <h3>5. Automatic Code Splitting</h3>
        <p>Next.js tự động chia code thành các chunks nhỏ hơn, chỉ load những gì cần thiết cho mỗi page. Điều này giúp giảm bundle size và cải thiện performance.</p>

        <h3>6. Image Optimization</h3>
        <p>Component <code>next/image</code> tự động optimize images, lazy loading, và responsive images. Điều này giúp cải thiện đáng kể performance và Core Web Vitals.</p>

        <h2>Cài đặt Next.js</h2>
        <p>Cách đơn giản nhất để bắt đầu với Next.js là sử dụng create-next-app:</p>
        <pre><code>npx create-next-app@latest my-app
cd my-app
npm run dev</code></pre>

        <p>Hoặc với TypeScript:</p>
        <pre><code>npx create-next-app@latest my-app --typescript</code></pre>

        <h2>Cấu trúc dự án Next.js</h2>
        <h3>App Router (Next.js 13+)</h3>
        <p>Với App Router, cấu trúc dự án sẽ như sau:</p>
        <pre><code>app/
  layout.tsx        // Root layout
  page.tsx          // Home page
  about/
    page.tsx        // About page
  api/
    users/
      route.ts     // API endpoint</code></pre>

        <h3>Pages Router (Legacy)</h3>
        <p>Với Pages Router, cấu trúc sẽ là:</p>
        <pre><code>pages/
  index.js         // Home page
  about.js         // About page
  api/
    users.js       // API endpoint</code></pre>

        <h2>Các tính năng chính của Next.js</h2>
        <h3>1. Data Fetching</h3>
        <p>Next.js cung cấp nhiều cách để fetch data:</p>
        <ul>
          <li><strong>getServerSideProps:</strong> Fetch data trên server cho mỗi request</li>
          <li><strong>getStaticProps:</strong> Fetch data tại build time</li>
          <li><strong>getStaticPaths:</strong> Định nghĩa dynamic routes cho static pages</li>
          <li><strong>Server Components:</strong> Fetch data trực tiếp trong React Server Components</li>
        </ul>

        <h3>2. Middleware</h3>
        <p>Next.js Middleware cho phép bạn chạy code trước khi request hoàn thành. Bạn có thể sử dụng để authentication, redirects, hoặc rewrite requests.</p>

        <h3>3. Environment Variables</h3>
        <p>Next.js hỗ trợ environment variables với prefix <code>NEXT_PUBLIC_</code> cho client-side variables.</p>

        <h3>4. Deployment</h3>
        <p>Next.js có thể được deploy dễ dàng trên Vercel, Netlify, hoặc bất kỳ platform nào hỗ trợ Node.js.</p>

        <h2>Best Practices</h2>
        <ul>
          <li>Sử dụng Server Components khi có thể để giảm bundle size</li>
          <li>Optimize images với <code>next/image</code></li>
          <li>Sử dụng dynamic imports cho code splitting</li>
          <li>Leverage caching strategies cho better performance</li>
          <li>Sử dụng TypeScript cho type safety</li>
        </ul>

        <h2>Kết luận</h2>
        <p>Next.js là một framework mạnh mẽ giúp bạn xây dựng các ứng dụng web hiện đại với React. Với các tính năng như SSR, SSG, và API Routes, Next.js là lựa chọn tuyệt vời cho cả small projects và large-scale applications.</p>
      `,
    },
    {
      title: "TypeScript: Tại sao nên sử dụng trong dự án của bạn?",
      description: "Khám phá TypeScript - superset của JavaScript với type system mạnh mẽ, giúp viết code an toàn hơn, dễ maintain hơn và ít bug hơn.",
      author: "Vũ Đức Anh",
      date: "January 23, 2025",
      readTime: "12 min read",
      tags: ["TypeScript", "Programming", "Web Development"],
      slug: "typescript-tai-sao-nen-su-dung",
      image: "/blog/typescript_la_gi.webp",
      content: `
        <h2>Giới thiệu về TypeScript</h2>
        <p>TypeScript là một ngôn ngữ lập trình được phát triển bởi Microsoft, là một superset của JavaScript. TypeScript thêm static type checking vào JavaScript, giúp bạn phát hiện lỗi sớm hơn trong quá trình phát triển và làm cho code dễ maintain hơn.</p>

        <h2>TypeScript là gì?</h2>
        <p>TypeScript là JavaScript với type annotations. Code TypeScript được compile sang JavaScript, nên bạn có thể sử dụng TypeScript ở bất kỳ đâu JavaScript có thể chạy. TypeScript không thay đổi runtime behavior của JavaScript - nó chỉ thêm type checking tại compile time.</p>

        <h2>Tại sao nên sử dụng TypeScript?</h2>
        <h3>1. Type Safety</h3>
        <p>TypeScript giúp bạn phát hiện lỗi trước khi code chạy. Ví dụ:</p>
        <pre><code>// JavaScript - lỗi chỉ phát hiện khi chạy
function add(a, b) {
  return a + b;
}
add("5", "3"); // "53" - không phải 8!

// TypeScript - lỗi phát hiện ngay khi viết code
function add(a: number, b: number): number {
  return a + b;
}
add("5", "3"); // Error: Argument of type 'string' is not assignable to parameter of type 'number'</code></pre>

        <h3>2. Better IDE Support</h3>
        <p>Với TypeScript, IDE có thể cung cấp:</p>
        <ul>
          <li>Autocomplete thông minh hơn</li>
          <li>Refactoring an toàn hơn</li>
          <li>IntelliSense tốt hơn</li>
          <li>Error detection ngay khi gõ code</li>
        </ul>

        <h3>3. Self-Documenting Code</h3>
        <p>Types trong TypeScript giúp code tự document. Bạn không cần đọc implementation để biết function nhận parameters gì và trả về gì.</p>
        <pre><code>// Rõ ràng hơn nhiều so với JSDoc comments
function calculateTotal(items: Item[], discount: number): number {
  // ...
}</code></pre>

        <h3>4. Easier Refactoring</h3>
        <p>Khi refactor code, TypeScript sẽ báo lỗi ở tất cả các nơi bị ảnh hưởng, giúp bạn không bỏ sót.</p>

        <h3>5. Better for Large Projects</h3>
        <p>TypeScript đặc biệt hữu ích cho large codebases và team lớn. Nó giúp maintain code dễ dàng hơn và giảm bugs.</p>

        <h2>Các tính năng chính của TypeScript</h2>
        <h3>1. Basic Types</h3>
        <pre><code>let name: string = "Vũ Đức Anh";
let age: number = 20;
let isStudent: boolean = true;
let hobbies: string[] = ["Coding", "Soccer"];
let user: { name: string; age: number } = { name: "Vũ Đức Anh", age: 20 };</code></pre>

        <h3>2. Interfaces</h3>
        <pre><code>interface User {
  id: number;
  name: string;
  email: string;
  age?: number; // Optional property
}

const user: User = {
  id: 1,
  name: "Vũ Đức Anh",
  email: "vng15960@gmail.com"
};</code></pre>

        <h3>3. Types</h3>
        <pre><code>type Status = "pending" | "approved" | "rejected";
type ID = string | number;

interface Request {
  id: ID;
  status: Status;
}</code></pre>

        <h3>4. Generics</h3>
        <pre><code>function identity&lt;T&gt;(arg: T): T {
  return arg;
}

const num = identity&lt;number&gt;(42);
const str = identity&lt;string&gt;("Hello");</code></pre>

        <h3>5. Utility Types</h3>
        <pre><code>interface User {
  id: number;
  name: string;
  email: string;
}

// Partial - tất cả properties đều optional
type PartialUser = Partial&lt;User&gt;;

// Pick - chỉ lấy một số properties
type UserPreview = Pick&lt;User, "id" | "name"&gt;;

// Omit - bỏ một số properties
type UserWithoutEmail = Omit&lt;User, "email"&gt;;</code></pre>

        <h2>Cài đặt TypeScript</h2>
        <pre><code>npm install -D typescript @types/node @types/react @types/react-dom

# Tạo tsconfig.json
npx tsc --init</code></pre>

        <h2>TypeScript với React</h2>
        <pre><code>interface Props {
  title: string;
  count: number;
  onClick: () => void;
}

const Button: React.FC&lt;Props&gt; = ({ title, count, onClick }) => {
  return (
    &lt;button onClick={onClick}&gt;
      {title} ({count})
    &lt;/button&gt;
  );
};</code></pre>

        <h2>TypeScript với Next.js</h2>
        <p>Next.js có hỗ trợ TypeScript built-in. Chỉ cần tạo file <code>.ts</code> hoặc <code>.tsx</code> và Next.js sẽ tự động compile.</p>
        <pre><code>// pages/about.tsx
import { NextPage } from 'next';

interface AboutProps {
  title: string;
}

const About: NextPage&lt;AboutProps&gt; = ({ title }) => {
  return &lt;h1&gt;{title}&lt;/h1&gt;;
};

export default About;</code></pre>

        <h2>Best Practices</h2>
        <ul>
          <li>Bắt đầu với <code>strict: true</code> trong tsconfig.json</li>
          <li>Sử dụng interfaces cho objects, types cho unions và intersections</li>
          <li>Avoid <code>any</code> type - sử dụng <code>unknown</code> nếu cần</li>
          <li>Leverage type inference khi có thể</li>
          <li>Sử dụng utility types để giảm code duplication</li>
        </ul>

        <h2>Kết luận</h2>
        <p>TypeScript là một công cụ mạnh mẽ giúp bạn viết code JavaScript an toàn và maintainable hơn. Mặc dù có learning curve ban đầu, nhưng lợi ích lâu dài là rất lớn, đặc biệt cho các dự án lớn và team lớn.</p>
      `,
    },
    {
      title: "Express.js: Xây dựng RESTful API với Node.js",
      description: "Học cách sử dụng Express.js để xây dựng RESTful APIs mạnh mẽ, xử lý routes, middleware, và kết nối với database.",
      author: "Vũ Đức Anh",
      date: "January 22, 2025",
      readTime: "14 min read",
      tags: ["Express.js", "Node.js", "Backend", "API"],
      slug: "expressjs-xay-dung-restful-api",
      image: "/blog/expressjs_la_gi.webp",
      content: `
        <h2>Giới thiệu về Express.js</h2>
        <p>Express.js là một web framework nhỏ, nhanh và unopinionated cho Node.js. Nó cung cấp một bộ tính năng mạnh mẽ để xây dựng web applications và APIs. Express.js là framework phổ biến nhất cho Node.js, được sử dụng bởi hàng triệu developers trên toàn thế giới.</p>

        <h2>Express.js là gì?</h2>
        <p>Express.js là một minimal và flexible Node.js web application framework cung cấp một bộ tính năng mạnh mẽ cho web và mobile applications. Nó giúp đơn giản hóa việc tạo ra các web servers và APIs.</p>

        <h2>Tại sao nên sử dụng Express.js?</h2>
        <h3>1. Đơn giản và dễ học</h3>
        <p>Express.js có API đơn giản và dễ hiểu, phù hợp cho cả beginners và experienced developers.</p>

        <h3>2. Middleware System</h3>
        <p>Express.js có hệ thống middleware mạnh mẽ cho phép bạn xử lý requests ở nhiều stages khác nhau.</p>

        <h3>3. Routing</h3>
        <p>Express.js cung cấp routing system linh hoạt và mạnh mẽ để định nghĩa endpoints.</p>

        <h3>4. Large Ecosystem</h3>
        <p>Có hàng nghìn middleware và packages tương thích với Express.js.</p>

        <h2>Cài đặt Express.js</h2>
        <pre><code>npm init -y
npm install express
npm install -D @types/express typescript ts-node nodemon</code></pre>

        <h2>Tạo Server cơ bản</h2>
        <pre><code>const express = require('express');
const app = express();
const PORT = 3000;

app.get('/', (req, res) => {
  res.send('Hello World!');
});

app.listen(PORT, () => {
  console.log(\`Server is running on http://localhost:\${PORT}\`);
});</code></pre>

        <h2>Routing trong Express.js</h2>
        <h3>Basic Routes</h3>
        <pre><code>// GET request
app.get('/users', (req, res) => {
  res.json({ users: [...] });
});

// POST request
app.post('/users', (req, res) => {
  // Create new user
  res.status(201).json({ message: 'User created' });
});

// PUT request
app.put('/users/:id', (req, res) => {
  const { id } = req.params;
  // Update user
  res.json({ message: \`User \${id} updated\` });
});

// DELETE request
app.delete('/users/:id', (req, res) => {
  const { id } = req.params;
  // Delete user
  res.json({ message: \`User \${id} deleted\` });
});</code></pre>

        <h3>Route Parameters</h3>
        <pre><code>app.get('/users/:id', (req, res) => {
  const userId = req.params.id;
  res.json({ userId });
});

// Multiple parameters
app.get('/users/:userId/posts/:postId', (req, res) => {
  const { userId, postId } = req.params;
  res.json({ userId, postId });
});</code></pre>

        <h3>Query Parameters</h3>
        <pre><code>app.get('/search', (req, res) => {
  const { q, page, limit } = req.query;
  res.json({ query: q, page, limit });
});
// GET /search?q=javascript&page=1&limit=10</code></pre>

        <h2>Middleware trong Express.js</h2>
        <p>Middleware là functions có access đến request object (req), response object (res), và next function trong request-response cycle.</p>

        <h3>Built-in Middleware</h3>
        <pre><code>// Parse JSON bodies
app.use(express.json());

// Parse URL-encoded bodies
app.use(express.urlencoded({ extended: true }));

// Serve static files
app.use(express.static('public'));</code></pre>

        <h3>Custom Middleware</h3>
        <pre><code>// Logging middleware
const logger = (req, res, next) => {
  console.log(\`\${req.method} \${req.path}\`);
  next();
};

app.use(logger);

// Authentication middleware
const authenticate = (req, res, next) => {
  const token = req.headers.authorization;
  if (!token) {
    return res.status(401).json({ error: 'Unauthorized' });
  }
  // Verify token...
  next();
};

app.get('/protected', authenticate, (req, res) => {
  res.json({ message: 'Protected route' });
});</code></pre>

        <h2>Error Handling</h2>
        <pre><code>// Error handling middleware
app.use((err, req, res, next) => {
  console.error(err.stack);
  res.status(500).json({
    error: 'Something went wrong!',
    message: err.message
  });
});

// Async error handling
const asyncHandler = (fn) => (req, res, next) => {
  Promise.resolve(fn(req, res, next)).catch(next);
};

app.get('/users/:id', asyncHandler(async (req, res) => {
  const user = await User.findById(req.params.id);
  if (!user) {
    throw new Error('User not found');
  }
  res.json(user);
}));</code></pre>

        <h2>Kết nối Database</h2>
        <h3>Với MongoDB (Mongoose)</h3>
        <pre><code>const mongoose = require('mongoose');

mongoose.connect('mongodb://localhost:27017/myapp');

const UserSchema = new mongoose.Schema({
  name: String,
  email: String,
  age: Number
});

const User = mongoose.model('User', UserSchema);

app.get('/users', async (req, res) => {
  const users = await User.find();
  res.json(users);
});</code></pre>

        <h3>Với PostgreSQL</h3>
        <pre><code>const { Pool } = require('pg');
const pool = new Pool({
  user: 'postgres',
  host: 'localhost',
  database: 'myapp',
  password: 'password',
  port: 5432,
});

app.get('/users', async (req, res) => {
  const result = await pool.query('SELECT * FROM users');
  res.json(result.rows);
});</code></pre>

        <h2>Best Practices</h2>
        <ul>
          <li>Sử dụng environment variables cho configuration</li>
          <li>Organize routes vào separate files</li>
          <li>Sử dụng middleware cho cross-cutting concerns</li>
          <li>Implement proper error handling</li>
          <li>Validate input data</li>
          <li>Use HTTPS in production</li>
          <li>Implement rate limiting</li>
          <li>Use helmet.js cho security headers</li>
        </ul>

        <h2>Kết luận</h2>
        <p>Express.js là một framework mạnh mẽ và linh hoạt cho việc xây dựng web applications và APIs với Node.js. Với ecosystem phong phú và community lớn, Express.js là lựa chọn tuyệt vời cho backend development.</p>
      `,
    },
    {
      title: "Next.js App Router vs Pages Router: Nên chọn cái nào?",
      description: "So sánh chi tiết giữa App Router và Pages Router trong Next.js, giúp bạn quyết định nên sử dụng cái nào cho dự án của mình.",
      author: "Vũ Đức Anh",
      date: "January 21, 2025",
      readTime: "11 min read",
      tags: ["Next.js", "React", "Web Development"],
      slug: "nextjs-app-router-vs-pages-router",
      image: "/blog/nextjs_ar_la_gi.png",
      content: `
        <h2>Giới thiệu</h2>
        <p>Next.js có hai hệ thống routing chính: Pages Router (truyền thống) và App Router (mới, từ Next.js 13+). Mỗi cái có ưu và nhược điểm riêng. Trong bài viết này, chúng ta sẽ so sánh chi tiết để giúp bạn chọn đúng cho dự án của mình.</p>

        <h2>Pages Router (Traditional)</h2>
        <h3>Cấu trúc</h3>
        <pre><code>pages/
  index.js          → /
  about.js          → /about
  blog/
    index.js        → /blog
    [slug].js       → /blog/:slug
  api/
    users.js        → /api/users</code></pre>

        <h3>Ưu điểm</h3>
        <ul>
          <li>Đơn giản và dễ hiểu</li>
          <li>Stable và mature</li>
          <li>Nhiều tài liệu và tutorials</li>
          <li>Compatible với hầu hết React libraries</li>
          <li>Không cần học concepts mới</li>
        </ul>

        <h3>Nhược điểm</h3>
        <ul>
          <li>Không hỗ trợ React Server Components</li>
          <li>Layout nesting phức tạp hơn</li>
          <li>Loading states phải tự implement</li>
          <li>Streaming không được optimize tốt</li>
        </ul>

        <h2>App Router (New)</h2>
        <h3>Cấu trúc</h3>
        <pre><code>app/
  layout.tsx        → Root layout
  page.tsx          → Home page
  about/
    page.tsx        → /about
  blog/
    layout.tsx      → Blog layout
    page.tsx        → /blog
    [slug]/
      page.tsx      → /blog/:slug
  api/
    users/
      route.ts      → /api/users</code></pre>

        <h3>Ưu điểm</h3>
        <ul>
          <li>Hỗ trợ React Server Components</li>
          <li>Nested layouts dễ dàng</li>
          <li>Built-in loading states và error boundaries</li>
          <li>Streaming và Suspense tốt hơn</li>
          <li>Data fetching đơn giản hơn</li>
          <li>Better performance với Server Components</li>
        </ul>

        <h3>Nhược điểm</h3>
        <ul>
          <li>Learning curve cao hơn</li>
          <li>Một số libraries chưa tương thích</li>
          <li>Concepts mới cần học (Server/Client Components)</li>
          <li>Mới hơn nên ít tài liệu hơn</li>
        </ul>

        <h2>So sánh chi tiết</h2>
        <h3>1. Data Fetching</h3>
        <p><strong>Pages Router:</strong></p>
        <pre><code>export async function getServerSideProps() {
  const data = await fetchData();
  return { props: { data } };
}</code></pre>

        <p><strong>App Router:</strong></p>
        <pre><code>// Server Component - fetch trực tiếp
async function Page() {
  const data = await fetchData();
  return &lt;div&gt;{data}&lt;/div&gt;;
}</code></pre>

        <h3>2. Layouts</h3>
        <p><strong>Pages Router:</strong> Phải wrap mỗi page với layout component.</p>
        <p><strong>App Router:</strong> Layout tự động được apply với file <code>layout.tsx</code>.</p>

        <h3>3. Loading States</h3>
        <p><strong>Pages Router:</strong> Phải tự implement với router events.</p>
        <p><strong>App Router:</strong> Có <code>loading.tsx</code> file tự động.</p>

        <h3>4. Error Handling</h3>
        <p><strong>Pages Router:</strong> Sử dụng <code>_error.js</code>.</p>
        <p><strong>App Router:</strong> Có <code>error.tsx</code> với error boundaries tốt hơn.</p>

        <h2>Khi nào nên dùng Pages Router?</h2>
        <ul>
          <li>Dự án đã có sẵn với Pages Router</li>
          <li>Team chưa quen với Server Components</li>
          <li>Cần compatibility với nhiều libraries cũ</li>
          <li>Dự án nhỏ, đơn giản</li>
        </ul>

        <h2>Khi nào nên dùng App Router?</h2>
        <ul>
          <li>Dự án mới bắt đầu</li>
          <li>Cần performance tốt nhất</li>
          <li>Muốn sử dụng React Server Components</li>
          <li>Dự án lớn với nhiều nested layouts</li>
          <li>Team sẵn sàng học concepts mới</li>
        </ul>

        <h2>Migration từ Pages Router sang App Router</h2>
        <p>Bạn có thể chạy cả hai trong cùng một project, nhưng không nên mix trong cùng một route. Migration nên được làm từng phần:</p>
        <ol>
          <li>Bắt đầu với routes mới sử dụng App Router</li>
          <li>Gradually migrate existing routes</li>
          <li>Test kỹ lưỡng sau mỗi migration</li>
        </ol>

        <h2>Kết luận</h2>
        <p>App Router là tương lai của Next.js và được khuyến nghị cho các dự án mới. Tuy nhiên, Pages Router vẫn là lựa chọn tốt cho các dự án hiện tại hoặc khi bạn cần stability và compatibility. Quyết định cuối cùng phụ thuộc vào nhu cầu cụ thể của dự án và team của bạn.</p>
      `,
    },
    {
      title: "TypeScript với Express.js: Xây dựng API type-safe",
      description: "Học cách kết hợp TypeScript và Express.js để xây dựng RESTful APIs an toàn, maintainable với type checking từ đầu đến cuối.",
      author: "Vũ Đức Anh",
      date: "January 19, 2025",
      readTime: "13 min read",
      tags: ["TypeScript", "Express.js", "Backend", "API"],
      slug: "typescript-expressjs-api-type-safe",
      image: "/blog/ts.png",
      content: `
        <h2>Giới thiệu</h2>
        <p>Kết hợp TypeScript với Express.js giúp bạn xây dựng APIs mạnh mẽ, type-safe và dễ maintain. Trong bài viết này, chúng ta sẽ học cách setup và sử dụng TypeScript với Express.js để tạo ra RESTful APIs với full type safety.</p>

        <h2>Tại sao TypeScript với Express.js?</h2>
        <ul>
          <li><strong>Type Safety:</strong> Phát hiện lỗi tại compile time</li>
          <li><strong>Better IDE Support:</strong> Autocomplete và IntelliSense tốt hơn</li>
          <li><strong>Self-Documenting:</strong> Types giúp code tự document</li>
          <li><strong>Refactoring:</strong> An toàn và dễ dàng hơn</li>
          <li><strong>Team Collaboration:</strong> Dễ hiểu code hơn trong team lớn</li>
        </ul>

        <h2>Setup Project</h2>
        <pre><code>npm init -y
npm install express
npm install -D typescript @types/express @types/node ts-node nodemon

# Tạo tsconfig.json
npx tsc --init</code></pre>

        <h2>TypeScript Configuration</h2>
        <pre><code>// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}</code></pre>

        <h2>Typing Express Request và Response</h2>
        <h3>Basic Types</h3>
        <pre><code>import { Request, Response, NextFunction } from 'express';

app.get('/users', (req: Request, res: Response) => {
  res.json({ users: [] });
});</code></pre>

        <h3>Custom Request Types</h3>
        <pre><code>interface AuthenticatedRequest extends Request {
  user?: {
    id: string;
    email: string;
    role: string;
  };
}

const authMiddleware = (req: AuthenticatedRequest, res: Response, next: NextFunction) => {
  // Authentication logic...
  req.user = { id: '1', email: 'user@example.com', role: 'admin' };
  next();
};

app.get('/profile', authMiddleware, (req: AuthenticatedRequest, res: Response) => {
  res.json({ user: req.user });
});</code></pre>

        <h2>Typing Route Handlers</h2>
        <pre><code>interface User {
  id: string;
  name: string;
  email: string;
}

interface CreateUserBody {
  name: string;
  email: string;
  password: string;
}

app.post('/users', async (req: Request&lt;{}, User, CreateUserBody&gt;, res: Response&lt;User&gt;) => {
  const { name, email, password } = req.body;
  
  // TypeScript knows the types!
  const newUser: User = {
    id: '1',
    name,
    email
  };
  
  res.status(201).json(newUser);
});</code></pre>

        <h2>Typing Route Parameters</h2>
        <pre><code>interface UserParams {
  id: string;
}

app.get('/users/:id', (req: Request&lt;UserParams&gt;, res: Response) => {
  const { id } = req.params; // TypeScript knows id is string
  res.json({ userId: id });
});</code></pre>

        <h2>Typing Query Parameters</h2>
        <pre><code>interface UserQuery {
  page?: string;
  limit?: string;
  search?: string;
}

app.get('/users', (req: Request&lt;{}, {}, {}, UserQuery&gt;, res: Response) => {
  const { page = '1', limit = '10', search } = req.query;
  const pageNum = parseInt(page);
  const limitNum = parseInt(limit);
  
  res.json({ page: pageNum, limit: limitNum, search });
});</code></pre>

        <h2>Error Handling với Types</h2>
        <pre><code>class AppError extends Error {
  statusCode: number;
  isOperational: boolean;

  constructor(message: string, statusCode: number) {
    super(message);
    this.statusCode = statusCode;
    this.isOperational = true;
    Error.captureStackTrace(this, this.constructor);
  }
}

const errorHandler = (
  err: AppError | Error,
  req: Request,
  res: Response,
  next: NextFunction
) => {
  if (err instanceof AppError) {
    return res.status(err.statusCode).json({
      error: err.message,
      statusCode: err.statusCode
    });
  }
  
  res.status(500).json({
    error: 'Internal Server Error',
    message: err.message
  });
};

app.use(errorHandler);</code></pre>

        <h2>Typing Middleware</h2>
        <pre><code>type Middleware = (
  req: Request,
  res: Response,
  next: NextFunction
) => void | Promise&lt;void&gt;;

const logger: Middleware = (req, res, next) => {
  console.log(\`\${req.method} \${req.path}\`);
  next();
};

const asyncHandler = &lt;T extends Request&gt;(
  fn: (req: T, res: Response, next: NextFunction) => Promise&lt;void&gt;
) => {
  return (req: T, res: Response, next: NextFunction) => {
    Promise.resolve(fn(req, res, next)).catch(next);
  };
};</code></pre>

        <h2>Validation với Zod</h2>
        <pre><code>import { z } from 'zod';

const createUserSchema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
  password: z.string().min(8)
});

type CreateUserInput = z.infer&lt;typeof createUserSchema&gt;;

const validate = (schema: z.ZodSchema) => {
  return (req: Request, res: Response, next: NextFunction) => {
    try {
      schema.parse(req.body);
      next();
    } catch (error) {
      res.status(400).json({ error: 'Validation failed', details: error });
    }
  };
};

app.post('/users', validate(createUserSchema), (req: Request&lt;{}, {}, CreateUserInput&gt;, res: Response) => {
  // req.body is now typed!
  res.json({ message: 'User created', user: req.body });
});</code></pre>

        <h2>Best Practices</h2>
        <ul>
          <li>Define types/interfaces cho tất cả request/response</li>
          <li>Sử dụng generic types cho reusable handlers</li>
          <li>Validate input với Zod hoặc similar libraries</li>
          <li>Create custom error classes với types</li>
          <li>Use strict mode trong tsconfig.json</li>
          <li>Organize types vào separate files</li>
        </ul>

        <h2>Kết luận</h2>
        <p>TypeScript với Express.js là một combination mạnh mẽ giúp bạn xây dựng APIs an toàn và maintainable. Mặc dù setup ban đầu có thể phức tạp hơn một chút, nhưng lợi ích lâu dài về type safety và developer experience là rất đáng giá.</p>
      `,
    },
  ],

  // Certifications/Badges Information
  certifications: [
    {
      title: "JavaScript Essentials 1",
      issuer: "Cisco Networking Academy",
      issued: "2025",
      image: "/badge/JS1.png",
      link: "/linkBadge/JS1.pdf",
      description: "Statement of Achievement for completing the JavaScript Essentials 1 course, provided by Cisco Networking Academy in collaboration with OpenEDG JavaScript Institute.",
    },
    {
      title: "JavaScript Essentials 2",
      issuer: "Cisco Networking Academy",
      issued: "2025",
      image: "/badge/JS2.png",
      link: "/linkBadge/JS2.pdf",
      description: "Statement of Achievement for completing the JavaScript Essentials 2 course, provided by Cisco Networking Academy in collaboration with OpenEDG JavaScript Institute.",
    },
    {
      title: "Networking Basics",
      issuer: "Cisco Networking Academy",
      issued: "2025",
      image: "/badge/Network.png",
      link: "/linkBadge/Network.pdf",
      description: "Certificate of Course Completion for completing the Networking Basics course.",
    },
  ],

  // More Links Information
  moreLinks: [
    {
      title: "Github Repositories",
      description:
        "My Github Repositories",
      link: "https://github.com/vuducanh221104?tab=repositories",
    },
    {
      title: "Buy Me a coffee",
      description:
        "Supports my content quality, new topics, and project creation.",
      link: "https://buymeacoffee.com/vng15960d",
    },
  ],

  // API Keys (should be in .env but referenced here)
  apiKeys: {
    resendApiKey: "YOUR_RESEND_API_KEY", // This should be loaded from environment variables in a real app
  },
};
